# Use map and a lambda function to find the maximum x coordinate (the 0-th coordinate) in a list of points.
# You will need to apply max to the result of the map

arr = [45, 23, 87, 11, 23, 89]
print(max(arr))